<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>NeoTunnel</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>SYSX</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<!-- Required for a network system extension to activate; without it macOS
	     rejects activation ("…network_extension category require the presence of
	     the 'NSSystemExtensionUsageDescription' property"). Needed in the
	     extension's own Info.plist, not just the containing app's. -->
	<key>NSSystemExtensionUsageDescription</key>
	<string>Junctus Neo uses a system extension to route your network traffic through the privacy overlay.</string>
	<key>NetworkExtension</key>
	<dict>
		<!-- Deliberately NO NEMachServiceName: it forces the mach service name to be
		     prefixed by an App Group the extension belongs to, but this packet tunnel
		     has no app group (the Developer ID profile grants none), so setting it
		     made the NetworkExtension category reject activation ("extension category
		     returned error"). A packet-tunnel provider is reached via
		     NETunnelProviderManager (by provider bundle id), so it isn't needed. -->
		<key>NEProviderClasses</key>
		<dict>
			<key>com.apple.networkextension.packet-tunnel</key>
			<string>$(PRODUCT_MODULE_NAME).PacketTunnelProvider</string>
		</dict>
	</dict>
</dict>
</plist>
